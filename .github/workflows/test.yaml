name: test

on:
    workflow_dispatch:

jobs:
    hello_world:
        runs-on: ubuntu-22.04
        steps:
            - run: echo "hello world"

    starrtttt56t6ttt55ttrtt5:
        runs-on: ubuntu-latest
        steps:
            - run: print("hello world")
              shell: python
        needs:
            - hello_world